import { ChargeHoraireEffectif } from './charge-horaire-effectif';

describe('ChargeHoraireEffectif', () => {
  it('should create an instance', () => {
    expect(new ChargeHoraireEffectif()).toBeTruthy();
  });
});
